# Copy to .env.local for local development if you need to set runtime config.
# The app currently has no required env vars; access control lives at the
# deployment layer (Render's gate / Cloudflare Access / VPN / etc.).

# NODE_ENV is set automatically by Next.js / Render — no need to pin it here.
